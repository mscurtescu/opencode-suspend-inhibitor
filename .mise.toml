[tools]
# Task runner (Taskfile.yml)
task = "3.43.3"

# Beads
"github:gastownhall/beads" = "1.0.4"

# Bun - OpenCode loads plugins via Bun; used locally for install/typecheck
bun = "1.3.14"

# beads-ui (web dashboard)
"npm:beads-ui" = "0.12.1"

# Git hooks
lefthook = "2.1.10"
